import { Header } from '@components/common/header';
import { SafeInfoBody } from '@components/safeInfo';

export const SafeInfo = () => {
    return (
        <>
            <Header></Header>
            <SafeInfoBody></SafeInfoBody>
        </>
    );
};
