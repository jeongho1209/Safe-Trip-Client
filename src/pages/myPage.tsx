import { Header } from '@components/common/header';
import { MyProfile } from '@components/myprofile';

export const MyPage = () => {
    return (
        <>
            <Header></Header>
            <MyProfile></MyProfile>
        </>
    );
};
